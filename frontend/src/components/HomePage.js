import {
    Box,
    Typography,
} from "@mui/material";

function HomePage() {

    return (
        <Box display={"flex"} flexDirection={"column"} width={"30%"}>
        <Typography> test </Typography>
        </Box>
    )
}

export default HomePage;
